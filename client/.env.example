# WebSocket server URL — set this to wherever the server is running.
# For local development:
VITE_WS_URL=ws://localhost:3001
# For production (AWS), replace with your EC2 public IP:
# VITE_WS_URL=ws://<EC2_PUBLIC_IP>:3001
