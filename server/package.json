{
  "name": "live-sync-sticky-board-server",
  "version": "1.0.0",
  "description": "WebSocket server for the Live-Sync Sticky Board \u2014 handles real-time note sync and JSON file persistence.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test src/store.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ws": "^8.18.0"
  }
}
